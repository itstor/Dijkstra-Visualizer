namespace states {
    public enum CursorState
    {
        Select,
        Connect,
        Add,
        FindPath,
    }

    public enum ToolsType
    {
        Select,
        Connect,
        Add,
        FindPath,
    }
}